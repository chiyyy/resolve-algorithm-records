# 네트워크

- Platform: PROGRAMMERS
- Difficulty: MEDIUM
- URL: https://school.programmers.co.kr/learn/courses/30/lessons/43162

## Code
```java
def solution(n, computers):
    visited = [False] * n
    count = 0
    def dfs(node):
        visited[node] = True
        for i in range(n):
            if computers[node][i] == 1 and not visited[i]:
                dfs(i)
    for i in range(n):
        if not visited[i]:
            dfs(i)
            count += 1
    return count
```

## Note
## Union-Find vs DFS
DFS로 연결 컴포넌트 수를 세는 방식으로 풀었지만, Union-Find로 더 효율적으로 풀 수 있음.

###재풀이 필요
- Union-Find 구현 연습
- 재귀 깊이 제한 주의 (sys.setrecursionlimit)